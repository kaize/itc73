%table.table.table-striped
  %thead
    %tr
      %th= sort_link @q, :name

  %tbody
  - @levels.each do |n|
    %tr
      %td= n.name
      %td
        .pull-right.btn-group
          = link_to edit_admin_course_level_path(n), class: 'btn btn-mini btn-primary' do
            %i.icon-pencil
            = t(:edit)
          = link_to admin_course_level_path(n), method: :delete,
            class: 'btn btn-mini btn-danger',
            data: { confirm: t('.delete_confirm') } do
            %i.icon-trash

.pagination
  = paginate @levels

= render 'sidebar'
