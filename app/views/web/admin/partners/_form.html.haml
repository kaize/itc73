= simple_form_for @partner, url: url do |f|
  = f.input :name, input_html: { class: 'input-xxlarge' }
  = f.input :url, input_html: { class: 'input-xxlarge' }
  = f.input :image, as: :image_preview
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('.cancel'), admin_partners_path, class: 'btn'
    - if @partner.can_destroy?
      = link_to t('.delete'), admin_partner_path(@partner), method: :delete,
        class: 'btn btn-danger pull-right',
        data: { confirm: t('.delete_confirm') }
